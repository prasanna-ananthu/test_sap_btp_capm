service MyService @(path: 'MyService') {        //@path is to telling the name keep as MyService in the ouput path
 
    //function greetings(name:String(20)) returns String;

    function multiply(a:Integer, b:Integer) returns Integer;
}